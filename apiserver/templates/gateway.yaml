apiVersion: "networking.istio.io/v1alpha3"
kind: "Gateway"
metadata:
  name: "{{ .Values.name }}"
  labels:
    app.kubernetes.io/name: "{{ .Values.name }}"
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      name: "grpc"
      number: {{ .Values.port }}
      protocol: "GRPC"
    hosts:
    - "*"
