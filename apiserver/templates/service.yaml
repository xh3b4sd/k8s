apiVersion: "v1"
kind: "Service"
metadata:
  name: "{{ .Values.name }}"
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: "LoadBalancer"
  selector:
    app.kubernetes.io/name: "{{ .Values.name }}"
  ports:
    - name: "grpc"
      port: {{ .Values.port }}
